/* General */
body {
  font-family: 'Arial', sans-serif;
  margin: 0;
  padding: 0;
  background: linear-gradient(to bottom, #ffffff, #d0e6f7);
}

header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 1em 2em;
  background-color: #007bff;
  color: white;
  box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
}

header .logo {
  font-size: 2em;
  font-weight: bold;
}

nav ul {
  list-style: none;
  display: flex;
}

nav ul li {
  margin: 0 1em;
}

nav ul li a {
  color: white;
  text-decoration: none;
  font-weight: bold;
}

.banner {
  text-align: center;
  padding: 4em 2em;
  background-color: #eaf6ff;
  box-shadow: 0px 6px 10px rgba(0, 0, 0, 0.1);
}

.productos .grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 2em;
  padding: 2em;
}

.categoria {
  text-align: center;
  background-color: white;
  border-radius: 12px;
  box-shadow: 0px 4px 20px rgba(0, 0, 0, 0.15);
  overflow: hidden;
  transition: transform 0.3s;
}

.categoria:hover {
  transform: scale(1.1);
}

.categoria img {
  width: 100%;
  border-bottom: 3px solid #007bff;
}

footer {
  text-align: center;
  padding: 1em;
  background-color: #007bff;
  color: white;
}
